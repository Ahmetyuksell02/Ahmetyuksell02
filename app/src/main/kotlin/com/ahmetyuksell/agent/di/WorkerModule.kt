package com.ahmetyuksell.agent.di

// WorkManager configuration is provided directly by AgentAiApp (Configuration.Provider).
// Hilt workers are registered via @HiltWorker + @AssistedInject on each Worker class.
// No additional module setup is required.
